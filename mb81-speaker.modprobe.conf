# Keep the HDA codec powered so the asserted CIR coefficients, GPIO0 amp gate and
# DigEn survive. Without this the codec drops to D3 a few seconds after audio stops
# and the internal-speaker path silently dies.
options snd_hda_intel power_save=0 power_save_controller=N
