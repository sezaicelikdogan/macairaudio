# MacBook8,1: the Wildcat Point LPSS DMA controller does not deliver UART
# interrupts for the BCM4350 Bluetooth controller. Keep its PCI driver from
# binding so 8250_lpss uses interrupt-driven PIO instead.
blacklist dw_dmac_pci
